func walkToggle() {
    moveForward()
    if isOnClosedSwitch {
        toggleSwitch()
        if isBlocked {
            turnLeft()
            
        } else {
            turnRight()
            
        }
    }
}

while !isOnGem {
    walkToggle()
    
} 
collectGem()
